### Task #1

The problems were generated by livewire upgrading to version 3. Moved UserTable.php from app/Http/Livewire to app/Livewire and added the live modifier to wire:model.debounce.300ms, because in Livewire v3 wire:model sends request only on page reload.

### Task #2

User::create([]) mass assigns attributes. I added 'email' to the $fillable array, which allows the 'email' attribute it to be used during mass assignment.

### Task #3

Refactored the user registration logic so that it is handled by the UserRegistrationService.

Created the UserServiceProvider and bound the UserRegistrationService there to adhere to Laravel best practices and allow future extension of application logic
